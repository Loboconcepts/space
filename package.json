{
  "name": "space",
  "version": "1.0.0",
  "description": "var rows = speeches.filter(ifTermExists); \t\tfunction ifTermExists(row) { \t\t  \treturn row.term == ui.toLowerCase().replace(/\\?|\\!|\\./g,''); \t\t}",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . --out=dist --asar --overwrite --darwin"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Loboconcepts/space.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Loboconcepts/space/issues"
  },
  "homepage": "https://github.com/Loboconcepts/space#readme",
  "dependencies": {
    
  },
  "devDependencies": {
    "electron": "^3.0.10",
    "gulp": "^3.9.1",
    "electron-packager": "^13.0.0"
  }
}
